import styled from 'styled-components';

const ArticleWrapper = styled.div`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  overflow: auto;
  height: 750px;
`;

export default ArticleWrapper;

